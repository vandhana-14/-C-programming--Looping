#include <stdio.h>
int sum(int n){
    if(n==1){
        return 1;
    }
    return n+sum(n-1);
}
int main() {
    // Write C code here
    // int n,i,s=0;
    // scanf("%d",&n);
    // for(i=1;i<=n;i++){
    //     s=s+i;
    // }
    // printf("%d",s);
    
    int n;
    scanf("%d",&n);
    sum(n);
    return 0;
}
