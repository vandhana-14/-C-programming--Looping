#include <stdio.h>

int main() {
  int n,x=0,y=1,z=1,s;
  scanf("%d",&n);
  printf("%d %d",x,y,z);
  n=n-3;
  while(n){
      s=x+y+z;
      printf(" %d",s);
      x=y;
      y=z;
      z=s;
      n--;
  }
    return 0;
}
