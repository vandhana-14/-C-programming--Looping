#include <stdio.h>

int main() {
 int n,temp,s,sum;
 scanf("%d",&n);
 int c=0;temp=n,sum=0;
 for(;n;n=n/10,c++);
 while (temp){
    s=temp%10;
    if(c%2==1){
    sum=sum+s;
    }
    temp/=10;
    c--;
    }
 printf("%d",sum);
    return 0;
}
